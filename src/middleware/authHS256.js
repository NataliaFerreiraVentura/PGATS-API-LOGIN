// middleware/authHS256.js
// Middleware de autenticação (opcional, para uso futuro)

function authHS256(req, res, next) {
    // Implementação futura para autenticação JWT HS256
    next();
}

module.exports = authHS256;
